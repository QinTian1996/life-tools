---
phase: "02"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/page.tsx"
autonomous: true
requirements:
  - "HOME-01"
user_setup: []

must_haves:
  truths:
    - "Home page displays a '聊天' button linking to /chat"
    - "'聊天' button is styled consistently with existing buttons (bg-amber-600)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page with '聊天' button added to navigation"
      min_lines: 25
  key_links:
    - from: "src/app/page.tsx"
      to: "/chat"
      via: "href in anchor tag"
      pattern: "href.*chat"
---

<objective>
Add a "聊天" (Chat) button to the home page at `/` that links to the `/chat` route. The button must be styled consistently with existing buttons using `bg-amber-600` matching the "算八字" button per D-16.

Purpose: Navigation from home page to chat feature
Output: Home page with "聊天" button
</objective>

<execution_context>
@/home/qt/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qt/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-chat-ui-home-page/02-CONTEXT.md

# D-16: "聊天" button styled consistently with existing buttons — use `bg-amber-600` to match "算八字" button styling

# Existing button styles from src/app/page.tsx:
# 算八字: px-6 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition-colors
# 今天吃什么: px-6 py-3 bg-orange-500 text-white rounded-lg hover:bg-orange-600 transition-colors
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "聊天" button to home page with amber styling</name>
  <files>src/app/page.tsx</files>
  <action>
Add a third button to the button group in `src/app/page.tsx`. The button group currently has "算八字" (bazi) and "今天吃什么" (eat). Add "聊天" following D-16:

```tsx
<a
  href="/chat"
  className="px-6 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition-colors"
>
  聊天
</a>
```

Place it in the button container `<div className="flex gap-4">` alongside the existing two buttons. The button should be styled exactly like "算八字" to maintain visual consistency per D-16.
</action>
  <verify>
    <automated>grep -c "href=\"/chat\"" src/app/page.tsx && grep -c "bg-amber-600" src/app/page.tsx</automated>
  </verify>
  <done>Home page displays "聊天" button with amber-600 styling, linking to /chat</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | Static link navigation — no data transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | None | Home page button | accept | Static navigation link with no user input processing |
</threat_model>

<verification>
- [ ] `src/app/page.tsx` contains `href="/chat"` link
- [ ] "聊天" button uses `bg-amber-600` class
- [ ] Three buttons visible on home page (算八字, 今天吃什么, 聊天)
</verification>

<success_criteria>
1. Home page shows three buttons in a row
2. "聊天" button styled with amber-600 matching "算八字"
3. Clicking "聊天" navigates to `/chat`
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-home-page/02-02-SUMMARY.md`
</output>
