---
phase: 09
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/bazi/roasts/route.ts
  - src/app/api/bazi/report/route.ts
  - src/lib/bazi/prompts/roasts.txt
  - src/lib/bazi/prompts/report-professional.txt
  - src/lib/bazi/prompts/report-detailed.txt
autonomous: true
requirements:
  - BZ-09
  - BZ-10
  - BZ-11
  - BZ-12
  - BZ-13
  - BZ-16
  - BZ-17
must_haves:
  truths:
    - "User submits form and immediately sees their input echoed back"
    - "User sees computed four pillars displayed while waiting for API"
    - "User sees rotating roast jokes during the wait (5s fade transitions)"
    - "User can cancel the in-flight API request at any time"
    - "After report returns, user sees a scrollable dual-version report"
    - "User can switch between 专业版 and 详解版 tabs instantly"
    - "LLM HTML content is sanitized before rendering"
    - "Re-submitting clears old report and starts fresh"
    - "Form collapses to compact summary after report generates"
  artifacts:
    - path: "src/app/api/bazi/roasts/route.ts"
      provides: "Fast roast jokes API (POST, returns {roasts: string[]})"
      min_lines: 50
    - path: "src/app/api/bazi/report/route.ts"
      provides: "Slow dual-version report API (POST, returns {professional, detailed})"
      min_lines: 80
    - path: "src/lib/bazi/prompts/roasts.txt"
      provides: "Prompt template for generating roast jokes"
      min_lines: 5
    - path: "src/lib/bazi/prompts/report-professional.txt"
      provides: "Prompt template for 专业版 report"
      min_lines: 10
    - path: "src/lib/bazi/prompts/report-detailed.txt"
      provides: "Prompt template for 详解版 report"
      min_lines: 10
  key_links:
    - from: "src/app/bazi/page.tsx"
      to: "/api/bazi/roasts"
      via: "fetch POST with AbortController signal"
    - from: "src/app/bazi/page.tsx"
      to: "/api/bazi/report"
      via: "fetch POST with same AbortController signal"
    - from: "BaziReportTab"
      to: "DOMPurify.sanitize()"
      via: "import and call before dangerouslySetInnerHTML"
---

<objective>
Build the backend API foundation for Phase 9: two API routes for roasts and reports, prompt template files, and DOMPurify installation. This plan delivers the data layer that the UI components in Wave 2 will consume.

Purpose: Provide the API endpoints and prompt templates that generate LLM content for the loading carousel and dual-version report.
Output: Two API routes + 3 prompt files + package installation
</objective>

<execution_context>
@/home/qt/projects/life-tools/.planning/phases/09-loading-report/09-CONTEXT.md
@/home/qt/projects/life-tools/.planning/phases/09-loading-report/09-SPEC.md
@/home/qt/projects/life-tools/src/app/api/chat/route.ts (adapt pattern)
</execution_context>

<context>
**Phase:** 09-loading-report
**Wave:** 1 (backend foundation)
**Requirements:** BZ-09, BZ-10, BZ-11, BZ-12, BZ-13, BZ-16, BZ-17

## Key Design Decisions (from 09-CONTEXT.md)

- **D-01:** Two separate routes: `POST /api/bazi/roasts` and `POST /api/bazi/report`
  - Roasts returns `{ roasts: string[] }` — fast (~2-5s)
  - Report returns `{ professional: string, detailed: string }` — slow (30-60s)
  - Both use `@ai-sdk/deepseek` with `deepseek('deepseek-v4-flash')` and `streamText`
  - Reuse existing DeepSeek API key from `.env.local` (`DEEPSEEK_API_KEY`)

- **D-02:** Prompt templates live in `src/lib/bazi/prompts/` as independent files

- **D-07:** `npm install dompurify @types/dompurify`

## Existing Patterns

From `src/app/api/chat/route.ts`:
- Uses `streamText` from `ai`
- Uses `deepseek` from `@ai-sdk/deepseek`
- Error handling with `mapErrorToCode()`
- Returns `result.toUIMessageStreamResponse()`

For roasts/report routes, adapt but return full JSON (non-streaming to client):
- Call `streamText()` but await the full `result.rawCall` or use `generateText` instead
- Return `Response.json()` with the structured data

## API Contract

### POST /api/bazi/roasts
Request body: `BirthInput` (year, month, day, hour, minute, calendar, timeMode, shichen, gender, name)

Response:
```json
{
  "roasts": [" roast joke 1 ", " roast joke 2 ", ...]
}
```

### POST /api/bazi/report
Request body: `BirthInput`

Response:
```json
{
  "professional": "<h2>专业版报告内容...</h2>",
  "detailed": "<h2>详解版报告内容...</h2>"
}
```

## Prompt File Structure

**roasts.txt:** Generate 5-8 short humorous "吐槽" about the person's 八字 characteristics. Format as a JSON array of strings.

**report-professional.txt:** Generate a professional-style 命理分析 report in HTML. Include sections for: 四柱分析, 大运走势, 十神论断, 格局判断, 调候用神, 形象比喻, 综合结论. Use project design tokens for styling.

**report-detailed.txt:** Generate a detailed plain-language explanation. Same sections as professional but written for beginners. Include more explanation of WHY rather than WHAT.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install DOMPurify</name>
  <files>package.json</files>
  <action>
Install DOMPurify and its TypeScript types for sanitizing LLM-generated HTML:

```bash
npm install dompurify @types/dompurify
```

This satisfies D-07 from the context. The package will be used in Wave 2 when building BaziReportTab.
  </action>
  <verify>
    <automated>grep -E '"dompurify"' package.json</automated>
  </verify>
  <done>DOMPurify installed with types</done>
</task>

<task type="auto">
  <name>Task 2: Create prompt template files</name>
  <files>
    - src/lib/bazi/prompts/roasts.txt
    - src/lib/bazi/prompts/report-professional.txt
    - src/lib/bazi/prompts/report-detailed.txt
  </files>
  <action>
Create `src/lib/bazi/prompts/` directory and three prompt template files:

**`roasts.txt`** — Roast jokes prompt:
```
你是一个幽默的八字大师，正在根据以下八字信息吐槽当事人。
八字信息：
- 四柱：{yearPillar}年 {monthPillar}月 {dayPillar}日 {hourPillar}时
- 性别：{gender}
- 姓名：{name}

请生成5-8条幽默吐槽，每条20-50字，用JSON数组格式返回：
["吐槽1", "吐槽2", ...]

要求：
- 语气轻松调侃但不过分刻薄
- 结合八字特点进行吐槽
- 适合在等待正式报告时展示
```

**`report-professional.txt`** — Professional report prompt:
```
你是一位深谙陆致极、韦千里命理传统的八字大师。根据以下八字信息：
{fourPillars}

请生成一份专业版命理分析报告，要求：

1. 格式：返回完整HTML片段，使用以下CSS类：`.bazi-report`, `.section`, `.pillar-table`, `.conclusion`
2. 结构包含：
   - 基础信息表（四柱、大运、十神）
   - 四柱强弱分析（年柱、月柱、日柱、时柱各自的作用）
   - 调候用神（根据出生月份判定需要什么来平衡）
   - 格局判断（根据日主和其他柱的关系判定）
   - 形象比喻（用一个自然界的形象来比喻这个八字）
   - 大运走势（简述未来10年趋势）
   - 综合结论（一段话总结）

3. 风格：专业、简洁、有深度
4. 字体：使用项目字体变量 var(--font-serif)
5. 颜色：使用项目颜色变量如 var(--text-primary)
```

**`report-detailed.txt`** — Detailed report prompt:
```
你是一位耐心的八字老师，正在向一个完全不懂八字的人解释一个人的命理。

八字信息：
{fourPillars}

请生成一份详解版报告，要求：

1. 格式：返回HTML片段，风格比专业版更口语化、易懂
2. 结构：与专业版相同（基础信息、四柱分析、调候、格局、形象、大运、结论）
3. 每部分都要解释"为什么"：
   - 不是只说"日主旺"，而要解释"为什么日主旺"
   - 不是只说"用神是火"，而要解释"为什么需要火"
4. 可加入适当的比喻和例子
5. 整体感觉：像一位老师在手把手教学生看自己的八字
```
  </action>
  <verify>
    <automated>ls src/lib/bazi/prompts/ && wc -l src/lib/bazi/prompts/*.txt</automated>
  </verify>
  <done>All three prompt template files exist with content</done>
</task>

<task type="auto">
  <name>Task 3: Create /api/bazi/roasts route</name>
  <files>src/app/api/bazi/roasts/route.ts</files>
  <action>
Create `src/app/api/bazi/roasts/route.ts`:

```typescript
import { generateText } from 'ai';
import { deepseek } from '@ai-sdk/deepseek';
import type { BirthInput } from '@/lib/bazi/types';

export const runtime = 'nodejs';
export const maxDuration = 30;

function mapErrorToCode(error: unknown): { error: string; code: string } {
  const message = error instanceof Error ? error.message : String(error);
  const lower = message.toLowerCase();

  if (lower.includes('rate')) {
    return { error: 'Rate limit exceeded', code: 'RATE_LIMITED' };
  }
  if (lower.includes('context') || lower.includes('token')) {
    return { error: 'Context length exceeded', code: 'CONTEXT_OVERFLOW' };
  }
  if (lower.includes('invalid') || lower.includes('validation')) {
    return { error: 'Invalid request', code: 'INVALID_REQUEST' };
  }
  return { error: 'Model error', code: 'MODEL_ERROR' };
}

function buildFourPillarsText(input: BirthInput, bazi: {
  yearPillar: { stemName: string; branchName: string };
  monthPillar: { stemName: string; branchName: string };
  dayPillar: { stemName: string; branchName: string };
  hourPillar: { stemName: string; branchName: string };
}): string {
  return `${bazi.yearPillar.stemName}${bazi.yearPillar.branchName}年 ${bazi.monthPillar.stemName}${bazi.monthPillar.branchName}月 ${bazi.dayPillar.stemName}${bazi.dayPillar.branchName}日 ${bazi.hourPillar.stemName}${bazi.hourPillar.branchName}时`;
}

async function getRoasts(input: BirthInput, fourPillarsText: string): Promise<string[]> {
  // Dynamically import the prompt file
  const { default: roastPrompt } = await import('@/lib/bazi/prompts/roasts.txt?raw');
  
  const prompt = roastPrompt
    .replace('{fourPillars}', fourPillarsText)
    .replace('{gender}', input.gender === 'male' ? '男' : '女')
    .replace('{name}', input.name || '当事人');

  const { text } = await generateText({
    model: deepseek('deepseek-v4-flash'),
    prompt,
    maxOutputTokens: 1000,
  });

  // Parse JSON array from response
  try {
    // The model returns a JSON array as text
    const cleaned = text.trim();
    // Handle cases where the model wraps the array in markdown code blocks
    const jsonMatch = cleaned.match(/\[[\s\S]*\]/);
    if (jsonMatch) {
      return JSON.parse(jsonMatch[0]);
    }
    return JSON.parse(cleaned);
  } catch {
    // If parsing fails, return the raw text as a single roast
    return [text.trim()];
  }
}

export async function POST(req: Request) {
  try {
    const body = await req.json() as BirthInput;

    // Validate required fields
    if (!body.year || !body.month || !body.day || !body.gender) {
      return Response.json(
        { error: 'Missing required fields: year, month, day, gender', code: 'INVALID_REQUEST' },
        { status: 400 }
      );
    }

    // Import calculator to get four pillars text
    const { computeBazi } = await import('@/lib/bazi/calculator');
    const baziResult = computeBazi(body);
    const fourPillarsText = buildFourPillarsText(body, baziResult);

    const roasts = await getRoasts(body, fourPillarsText);

    return Response.json({ roasts });
  } catch (error) {
    const { error: errorMessage, code } = mapErrorToCode(error);
    return Response.json({ error: errorMessage, code }, { status: 500 });
  }
}
```

**Note:** The `?raw` import for text files requires Next.js 16 with experimental features or using `fs.readFileSync`. If `?raw` doesn't work, use this alternative:

```typescript
import fs from 'fs';
import path from 'path';

function readPrompt(filename: string): string {
  return fs.readFileSync(
    path.join(process.cwd(), 'src/lib/bazi/prompts', filename),
    'utf-8'
  );
}
```
  </action>
  <verify>
    <automated>test -f src/app/api/bazi/roasts/route.ts && grep -c "export async function POST" src/app/api/bazi/roasts/route.ts</automated>
  </verify>
  <done>POST /api/bazi/roasts returns {roasts: string[]} in under 5s</done>
</task>

<task type="auto">
  <name>Task 4: Create /api/bazi/report route</name>
  <files>src/app/api/bazi/report/route.ts</files>
  <action>
Create `src/app/api/bazi/report/route.ts`:

```typescript
import { generateText } from 'ai';
import { deepseek } from '@ai-sdk/deepseek';
import type { BirthInput } from '@/lib/bazi/types';

export const runtime = 'nodejs';
export const maxDuration = 120;

function mapErrorToCode(error: unknown): { error: string; code: string } {
  const message = error instanceof Error ? error.message : String(error);
  const lower = message.toLowerCase();

  if (lower.includes('rate')) {
    return { error: 'Rate limit exceeded', code: 'RATE_LIMITED' };
  }
  if (lower.includes('context') || lower.includes('token')) {
    return { error: 'Context length exceeded', code: 'CONTEXT_OVERFLOW' };
  }
  if (lower.includes('invalid') || lower.includes('validation')) {
    return { error: 'Invalid request', code: 'INVALID_REQUEST' };
  }
  return { error: 'Model error', code: 'MODEL_ERROR' };
}

function buildFourPillarsText(bazi: {
  yearPillar: { stemName: string; branchName: string };
  monthPillar: { stemName: string; branchName: string };
  dayPillar: { stemName: string; branchName: string };
  hourPillar: { stemName: string; branchName: string };
  dayun: Array<{ age: number; stemName: string; branchName: string }>;
  shiShen: { yearStem: string; monthStem: string; dayStem: string; hourStem: string };
}): string {
  return `年柱：${bazi.yearPillar.stemName}${bazi.yearPillar.branchName}
月柱：${bazi.monthPillar.stemName}${bazi.monthPillar.branchName}
日柱：${bazi.dayPillar.stemName}${bazi.dayPillar.branchName}
时柱：${bazi.hourPillar.stemName}${bazi.hourPillar.branchName}
大运：${bazi.dayun.slice(0, 4).map(d => `${d.age}岁${d.stemName}${d.branchName}`).join('、')}
十神：年干${bazi.shiShen.yearStem}、月干${bazi.shiShen.monthStem}、日干${bazi.shiShen.dayStem}、时干${bazi.shiShen.hourStem}`;
}

async function generateReport(input: BirthInput, fourPillarsText: string, promptFile: string): Promise<string> {
  // If promptFile doesn't exist yet, use inline prompts as fallback
  let prompt: string;
  
  try {
    const fs = await import('fs');
    const path = await import('path');
    prompt = fs.readFileSync(
      path.join(process.cwd(), 'src/lib/bazi/prompts', promptFile),
      'utf-8'
    );
  } catch {
    // Fallback inline prompts
    if (promptFile === 'report-professional.txt') {
      prompt = `你是一位专业的八字命理师。生成专业HTML报告...`;
    } else {
      prompt = `你是一位耐心的八字老师。生成详细易懂HTML报告...`;
    }
  }

  const filledPrompt = prompt.replace('{fourPillars}', fourPillarsText)
    .replace('{gender}', input.gender === 'male' ? '男' : '女')
    .replace('{name}', input.name || '当事人');

  const { text } = await generateText({
    model: deepseek('deepseek-v4-flash'),
    prompt: filledPrompt,
    maxOutputTokens: 4000,
  });

  return text.trim();
}

export async function POST(req: Request) {
  try {
    const body = await req.json() as BirthInput;

    // Validate required fields
    if (!body.year || !body.month || !body.day || !body.gender) {
      return Response.json(
        { error: 'Missing required fields: year, month, day, gender', code: 'INVALID_REQUEST' },
        { status: 400 }
      );
    }

    // Compute Bazi for prompt substitution
    const { computeBazi } = await import('@/lib/bazi/calculator');
    const baziResult = computeBazi(body);
    const fourPillarsText = buildFourPillarsText(baziResult);

    // Generate both versions in parallel
    const [professional, detailed] = await Promise.all([
      generateReport(body, fourPillarsText, 'report-professional.txt'),
      generateReport(body, fourPillarsText, 'report-detailed.txt'),
    ]);

    return Response.json({ professional, detailed });
  } catch (error) {
    const { error: errorMessage, code } = mapErrorToCode(error);
    return Response.json({ error: errorMessage, code }, { status: 500 });
  }
}
```
  </action>
  <verify>
    <automated>test -f src/app/api/bazi/report/route.ts && grep -c "export async function POST" src/app/api/bazi/report/route.ts</automated>
  </verify>
  <done>POST /api/bazi/report returns {professional, detailed} HTML content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/bazi/roasts | Untrusted BirthInput crosses here |
| client → /api/bazi/report | Untrusted BirthInput crosses here |
| LLM output → client | Raw HTML crosses here (XSS risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | API request body | mitigate | Validate BirthInput fields (year 1900-2100, month 1-12, day 1-31, gender enum) at route entry |
| T-09-02 | XSS | Report HTML output | mitigate | DOMPurify sanitization is handled in Wave 2 BaziReportTab — API returns raw HTML |
| T-09-03 | Denial of Service | /api/bazi/report | mitigate | maxDuration=120s timeout, maxOutputTokens=4000 |
| T-09-04 | Information Disclosure | Error messages | accept | Error responses don't expose internal paths or API key details |
</threat_model>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `POST /api/bazi/roasts` with valid input returns 200 with {roasts: string[]}
3. `POST /api/bazi/roasts` with invalid input returns 400
4. `POST /api/bazi/report` with valid input returns 200 with {professional, detailed}
5. `POST /api/bazi/report` with invalid input returns 400
</verification>

<success_criteria>
- DOMPurify installed and listed in package.json
- Prompt files exist at src/lib/bazi/prompts/ with valid content
- /api/bazi/roasts route exists and returns roast jokes
- /api/bazi/report route exists and returns dual-version HTML
- All routes handle errors gracefully with appropriate status codes
</success_criteria>

<output>
After completion, create `.planning/phases/09-loading-report/09-01-SUMMARY.md`
</output>
