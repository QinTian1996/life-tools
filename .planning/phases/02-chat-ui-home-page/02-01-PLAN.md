---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/chat/page.tsx"
  - "src/components/chat/ChatUI.tsx"
  - "src/components/chat/MessageList.tsx"
  - "src/components/chat/MessageBubble.tsx"
  - "src/components/chat/ChatInput.tsx"
autonomous: true
requirements:
  - "CHAT-01"
  - "CHAT-02"
  - "CHAT-04"
  - "CHAT-05"
  - "CHAT-06"
user_setup: []

must_haves:
  truths:
    - "User can type a message and send it via Enter key or Send button"
    - "User messages appear right-aligned with amber background"
    - "AI responses appear left-aligned with cream/white background"
    - "Animated dots appear while AI is generating response"
    - "Red error banner appears above input when API error occurs"
    - "Chat auto-scrolls to bottom when user is near bottom, but respects scroll position when user scrolled up"
  artifacts:
    - path: "src/app/chat/page.tsx"
      provides: "Chat page route with useChat hook integration"
      min_lines: 20
    - path: "src/components/chat/ChatUI.tsx"
      provides: "Main chat container managing useChat state and layout"
      min_lines: 40
    - path: "src/components/chat/MessageList.tsx"
      provides: "Scrollable message list with smart auto-scroll (D-14)"
      min_lines: 50
    - path: "src/components/chat/MessageBubble.tsx"
      provides: "Individual message bubble with left/right alignment per D-10/D-11"
      min_lines: 30
    - path: "src/components/chat/ChatInput.tsx"
      provides: "Input area with textarea, amber send button (D-15), error banner (D-13)"
      min_lines: 40
  key_links:
    - from: "src/app/chat/page.tsx"
      to: "/api/chat"
      via: "useChat hook endpoint prop"
      pattern: "useChat.*endpoint"
    - from: "ChatUI.tsx"
      to: "MessageList.tsx"
      via: "messages prop + isLoading prop"
      pattern: "messages.*MessageList"
    - from: "MessageList.tsx"
      to: "MessageBubble.tsx"
      via: "map over messages"
      pattern: "messages\\.map.*MessageBubble"
    - from: "ChatUI.tsx"
      to: "ChatInput.tsx"
      via: "handleSubmit, isLoading, error props"
      pattern: "handleSubmit.*ChatInput"
---

<objective>
Create the complete Chat UI at `/chat` using `useChat` from `@ai-sdk/react`. This delivers user message input, streaming AI responses with animated dots, left/right aligned bubbles in warm amber style, smart auto-scroll, and error banner display.

Purpose: User-facing chat interface consuming Phase 1's `/api/chat` streaming endpoint
Output: Functional chat page with all message handling, styling, and error states
</objective>

<execution_context>
@/home/qt/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qt/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-chat-ui-home-page/02-CONTEXT.md
@src/app/api/chat/route.ts

# Key patterns from D-XX decisions:
# D-10: User messages RIGHT-aligned, AI responses LEFT-aligned
# D-11: User bubbles = amber bg, AI bubbles = cream/white bg
# D-12: Three bouncing dots animation during streaming
# D-13: Red error banner ABOVE input area (not as bubble)
# D-14: Smart scroll — auto-scroll only if user is near bottom
# D-15: Amber send button matching home page theme

# useChat API from @ai-sdk/react:
# - messages: array of {id, role, content, createdAt}
# - input: current input value
# - setInput: update input
# - isLoading: true while AI is generating
# - error: Error object if last request failed
# - handleSubmit: submit handler (e.g., handleSubmit(e) { e.preventDefault(); submit() })
# - submit(): send message to API
</context>

<interfaces>
<!-- Key types from useChat hook consumed by components -->

```typescript
// Message type from useChat
interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  createdAt?: Date;
}

// ChatUI receives these from useChat:
interface ChatUIProps {
  messages: Message[];
  isLoading: boolean;
  error: Error | null;
  input: string;
  setInput: (value: string) => void;
  handleSubmit: (e: React.FormEvent) => void;
  submit: () => void;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageBubble component with alignment and styling per D-10/D-11</name>
  <files>src/components/chat/MessageBubble.tsx</files>
  <action>
Create `src/components/chat/MessageBubble.tsx` with:

1. Props interface: `{ message: Message }` where Message has `{ id, role, content }`
2. Conditional styling based on `message.role`:
   - `role === 'user'`: right-aligned, amber background (`bg-amber-500` or `bg-amber-400`), white text, rounded-lg with br (border-radius) on left
   - `role === 'assistant'`: left-aligned, cream/white background (`bg-orange-50` or `bg-amber-50`), gray text, rounded-lg with bl on left
3. Container div with `flex` and `justify-end` (user) or `justify-start` (assistant)
4. Inner bubble div with `max-w-[80%]`, padding, message.content rendered as text

Use Tailwind classes for all styling. The bubble should look warm and inviting per the amber theme.
</action>
  <verify>
    <automated>grep -c "role === 'user'" src/components/chat/MessageBubble.tsx && grep -c "role === 'assistant'" src/components/chat/MessageBubble.tsx && grep -c "bg-amber" src/components/chat/MessageBubble.tsx</automated>
  </verify>
  <done>MessageBubble renders user messages right-aligned with amber background and AI messages left-aligned with cream/white background</done>
</task>

<task type="auto">
  <name>Task 2: Create MessageList component with smart auto-scroll per D-14</name>
  <files>src/components/chat/MessageList.tsx</files>
  <action>
Create `src/components/chat/MessageList.tsx` with:

1. Props: `{ messages: Message[], isLoading: boolean }`
2. Create a ref: `const containerRef = useRef<HTMLDivElement>(null)`
3. `useEffect` to handle smart auto-scroll:
   - Check if user is near bottom (within ~100px) using `containerRef.current?.scrollHeight - containerRef.current?.scrollTop - containerRef.current?.clientHeight <= 100`
   - If near bottom: `containerRef.current?.scrollTo({ top: containerRef.current.scrollHeight, behavior: 'smooth' })`
   - This respects user's reading position if they scrolled up (D-14)
4. Render loading indicator (animated dots) when `isLoading === true`:
   - Three dots in a row with CSS animation
   - Position below last message, left-aligned (AI style)
   - Use `@keyframes` or Tailwind `animate-bounce`/`animate-pulse`
5. Map over `messages` to render `<MessageBubble>` for each
6. Container: `flex-1 overflow-y-auto` for scrollable area

For the animated dots, use three span elements with staggered bounce animation:
```css
@keyframes bounce {
  0%, 80%, 100% { transform: translateY(0); }
  40% { transform: translateY(-8px); }
}
```
Apply with `animation: bounce 1.4s infinite ease-in-out both` and staggered delays (0s, 0.16s, 0.32s) via inline style.
</action>
  <verify>
    <automated>grep -c "scrollHeight" src/components/chat/MessageList.tsx && grep -c "MessageBubble" src/components/chat/MessageList.tsx && grep -c "animate" src/components/chat/MessageList.tsx</automated>
  </verify>
  <done>MessageList auto-scrolls only when user is near bottom, shows animated dots during loading</done>
</task>

<task type="auto">
  <name>Task 3: Create ChatInput component with amber send button and error banner per D-13/D-15</name>
  <files>src/components/chat/ChatInput.tsx</files>
  <action>
Create `src/components/chat/ChatInput.tsx` with:

1. Props: `{ input: string, setInput: (v: string) => void, onSubmit: () => void, isLoading: boolean, error: Error | null }`
2. Error banner (D-13): 
   - When `error !== null`, show red banner above input
   - Style: `bg-red-50 border border-red-200 text-red-700 px-4 py-2 rounded-lg mb-2`
   - Message: `error.message` or "发生错误，请重试"
   - The banner disappears automatically when error is cleared (next successful message)
3. Textarea:
   - Bind to `input` value via `value={input}` and `onChange={e => setInput(e.target.value)}`
   - Placeholder: "输入消息..."
   - Style: `w-full border border-gray-200 rounded-lg px-4 py-3 resize-none focus:outline-none focus:ring-2 focus:ring-amber-500`
   - Handle Enter key (not Shift+Enter) to submit:
     ```typescript
     onKeyDown={(e) => {
       if (e.key === 'Enter' && !e.shiftKey) {
         e.preventDefault();
         if (!isLoading && input.trim()) onSubmit();
       }
     }}
     ```
4. Send button (D-15):
   - Amber styled: `bg-amber-600 hover:bg-amber-700 text-white`
   - Text: "发送"
   - Disabled when `isLoading || !input.trim()` (per agent's discretion)
   - Style: `px-4 py-2 rounded-lg font-medium transition-colors`
5. Container: `border-t border-gray-100 p-4 bg-white`
</action>
  <verify>
    <automated>grep -c "error" src/components/chat/ChatInput.tsx && grep -c "bg-amber-600" src/components/chat/ChatInput.tsx && grep -c "onKeyDown" src/components/chat/ChatInput.tsx</automated>
  </verify>
  <done>ChatInput shows red error banner when API fails, has amber send button, Enter submits (not Shift+Enter)</done>
</task>

<task type="auto">
  <name>Task 4: Create ChatUI container component and wire useChat hook</name>
  <files>src/components/chat/ChatUI.tsx</files>
  <action>
Create `src/components/chat/ChatUI.tsx` with:

1. `'use client'` directive at top
2. Import and destructure from `useChat`:
   ```typescript
   const {
     messages,
     input,
     setInput,
     handleSubmit,
     isLoading,
     error,
     submit,
   } = useChat({
     api: '/api/chat',
   });
   ```
3. Custom submit handler that prevents default and calls submit:
   ```typescript
   const onSubmit = (e: React.FormEvent) => {
     e.preventDefault();
     submit();
   };
   ```
4. Render layout:
   ```tsx
   <div className="flex flex-col h-screen bg-gradient-to-b from-amber-50 to-orange-50">
     {/* Header */}
     <header className="bg-amber-600 text-white px-4 py-3 shadow-md">
       <h1 className="text-lg font-bold">聊天</h1>
     </header>
     
     {/* Messages */}
     <MessageList messages={messages} isLoading={isLoading} />
     
     {/* Input */}
     <ChatInput
       input={input}
       setInput={setInput}
       onSubmit={onSubmit}
       isLoading={isLoading}
       error={error}
     />
   </div>
   ```
5. Style the container with `flex flex-col h-screen` to fill viewport
</action>
  <verify>
    <automated>grep -c "useChat" src/components/chat/ChatUI.tsx && grep -c "MessageList" src/components/chat/ChatUI.tsx && grep -c "MessageInput" src/components/chat/ChatUI.tsx</automated>
  </verify>
  <done>ChatUI container uses useChat hook, renders header, message list, and input with all props correctly wired</done>
</task>

<task type="auto">
  <name>Task 5: Create chat page at /chat route</name>
  <files>src/app/chat/page.tsx</files>
  <action>
Create `src/app/chat/page.tsx`:

1. Export default function that renders `<ChatUI />`
2. Import ChatUI from `@/components/chat/ChatUI`
3. No `'use client'` needed here — ChatUI is already marked as client component
4. Simple one-liner: `<ChatUI />`
</action>
  <verify>
    <automated>grep -c "ChatUI" src/app/chat/page.tsx</automated>
  </verify>
  <done>Chat page exists at /chat and renders ChatUI component</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User input sent to /api/chat; AI responses received via SSE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ChatInput | mitigate | Input is sent as-is to API; useChat handles encoding |
| T-02-02 | Information Disclosure | Error display | mitigate | D-13: Show user-friendly error message, not raw stack traces |
</threat_model>

<verification>
- [ ] `src/app/chat/page.tsx` exists and renders ChatUI
- [ ] `src/components/chat/ChatUI.tsx` uses `useChat` with `api: '/api/chat'`
- [ ] User messages render right-aligned with amber background
- [ ] AI messages render left-aligned with cream/white background
- [ ] Animated dots appear when `isLoading === true`
- [ ] Error banner appears when `error !== null`
- [ ] Smart scroll works (only when near bottom)
- [ ] Enter key submits (Shift+Enter allowed for newline)
- [ ] Send button disabled when empty or loading
</verification>

<success_criteria>
1. Navigate to `/chat` — page loads without errors
2. Type a message and press Enter — message appears as right-aligned amber bubble
3. Wait for AI response — streaming text appears in left-aligned cream bubble
4. While loading — animated dots visible below last message
5. Scroll up mid-conversation — dots animation stops auto-scrolling
6. API error — red banner appears above input with error message
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-ui-home-page/02-01-SUMMARY.md`
</output>
