---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bazi/BaziReport.tsx
  - src/components/bazi/BaziReportTab.tsx
  - src/lib/bazi/download.ts
autonomous: true
requirements:
  - BZ-14
  - BZ-15

must_haves:
  truths:
    - "User can download report as HTML file via button click"
    - "Downloaded HTML uses CDN fonts and requires no external dependencies"
    - "Downloaded HTML follows project design language (warm minimalist aesthetic)"
    - "Report headings have clear visual hierarchy with primary color"
    - "Report tables have borders and alternating row styles"
  artifacts:
    - path: "src/components/bazi/BaziReport.tsx"
      provides: "Download button in tab bar"
      min_lines: 15
    - path: "src/components/bazi/BaziReportTab.tsx"
      provides: "Scoped CSS styles for report content"
      min_lines: 15
    - path: "src/lib/bazi/download.ts"
      provides: "Blob download utility and HTML template builder"
      exports:
        - "downloadReportAsHtml"
        - "buildDownloadTemplate"
  key_links:
    - from: "src/components/bazi/BaziReport.tsx"
      to: "src/lib/bazi/download.ts"
      via: "import downloadReportAsHtml"
    - from: "BaziReportTab"
      to: "DOMPurify sanitized content"
      via: "dangerouslySetInnerHTML"
---

<objective>
Add HTML report download functionality and polish report CSS styling to match project design language.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-download-polish/10-SPEC.md
@.planning/phases/10-download-polish/10-CONTEXT.md

**Locked Decisions (per D-01 through D-11 in CONTEXT.md):**
- D-01: "下载报告" button in BaziReport's tab bar, right side of 专业版/详解版 tabs
- D-02: Client-side Blob download — reads BaziReportTab sanitized HTML, wraps in template, triggers download
- D-03: Downloads the currently active tab's content
- D-04: BaziReportTab injects a `<style>` tag before the content, scoped to `.bazi-report` class
- D-05: Style rules use project CSS variables (var(--primary), var(--foreground), var(--card), var(--border), etc.)
- D-06: Target selectors: h2 (font-brand, text-2xl, primary color), h3 (font-semibold), table (borders, header bg), p (line-height), ul/ol/li
- D-07: Same style block is embedded in download template for offline viewing
- D-08: Full HTML document: `<!DOCTYPE html>`, `<meta charset=utf-8>`, `<meta viewport>`, `<title>` from tab name
- D-09: CDN font link for LXGW WenKai from jsDelivr
- D-10: Inline `<style>` block matching the scoped styles from BaziReportTab
- D-11: Footer with "—— 喵十七 · 八字命理" attribution

**Project design tokens (from globals.css):**
- --primary: oklch(0.769 0.188 70.08) (light) / oklch(0.828 0.189 84.43) (dark)
- --foreground: oklch(0.18 0.01 50) (light) / oklch(0.96 0.01 50) (dark)
- --card: oklch(0.98 0.01 50) (light) / oklch(0.25 0.01 50) (dark)
- --border: oklch(0.92 0.02 50) (light) / oklch(0.35 0.02 50) (dark)
- --font-brand: "LXGW WenKai", system-ui, sans-serif
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/bazi/BaziReport.tsx:
```typescript
interface BaziReportProps {
  professional: string;  // HTML content for 专业版 tab
  detailed: string;       // HTML content for 详解版 tab
  inputName?: string;
}
type TabVersion = 'professional' | 'detailed';
```

From src/components/bazi/BaziReportTab.tsx:
```typescript
interface BaziReportTabProps {
  content: string;           // Sanitized HTML content
  version: 'professional' | 'detailed';
}
// Uses DOMPurify.sanitize() with ALLOWED_TAGS: p, br, h1-h4, strong, b, em, i, u, table, thead, tbody, tr, th, td, span, div, ul, ol, li
// ALLOWED_ATTR: ['class', 'style']
// Renders with: className="bazi-report text-[var(--foreground)] leading-relaxed space-y-4"
```

CDN Font URL for LXGW WenKai (jsDelivr):
```
https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-regular.css
https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-light.css
https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-bold.css
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create download utility with Blob download and HTML template builder</name>
  <files>src/lib/bazi/download.ts</files>
  <read_first>
    - src/lib/bazi/download.ts (new file)
    - src/components/bazi/BaziReportTab.tsx (understand sanitized content structure)
  </read_first>
  <action>
Create src/lib/bazi/download.ts with two exported functions:

1. `downloadReportAsHtml(content: string, version: 'professional' | 'detailed', inputName?: string): void`
   - Creates a Blob from the HTML content wrapped in buildDownloadTemplate()
   - Triggers browser download with filename: `八字报告-{inputName || '未命名'}-{version === 'professional' ? '专业版' : '详解版'}.html`
   - Uses URL.createObjectURL() and programmatic <a> click

2. `buildDownloadTemplate(content: string, title: string, version: 'professional' | 'detailed', inputName?: string): string`
   - Returns a complete HTML document string:
   ```html
   <!DOCTYPE html>
   <html lang="zh-CN">
   <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>${title}</title>
     <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-regular.css">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-light.css">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/lxgwwenkai-bold.css">
     <style>
       /* CSS variables for light mode */
       :root {
         --primary: oklch(0.769 0.188 70.08);
         --foreground: oklch(0.18 0.01 50);
         --card: oklch(0.98 0.01 50);
         --border: oklch(0.92 0.02 50);
         --font-brand: "LXGW WenKai", system-ui, sans-serif;
       }
       /* Dark mode */
       @media (prefers-color-scheme: dark) {
         :root {
           --primary: oklch(0.828 0.189 84.43);
           --foreground: oklch(0.96 0.01 50);
           --card: oklch(0.25 0.01 50);
           --border: oklch(0.35 0.02 50);
         }
       }
       /* Scoped styles matching BaziReportTab */
       .bazi-report {
         font-family: var(--font-brand);
         color: var(--foreground);
         line-height: 1.7;
       }
       .bazi-report h2 {
         font-family: var(--font-brand);
         font-size: 1.5rem;
         font-weight: 700;
         color: var(--primary);
         margin: 1.5rem 0 0.75rem;
       }
       .bazi-report h3 {
         font-size: 1.125rem;
         font-weight: 600;
         margin: 1.25rem 0 0.5rem;
       }
       .bazi-report p {
         margin: 0.75rem 0;
         line-height: 1.7;
       }
       .bazi-report table {
         width: 100%;
         border-collapse: collapse;
         margin: 1rem 0;
       }
       .bazi-report th, .bazi-report td {
         border: 1px solid var(--border);
         padding: 0.5rem 0.75rem;
         text-align: left;
       }
       .bazi-report th {
         background: var(--primary);
         color: white;
         font-weight: 600;
       }
       .bazi-report tr:nth-child(even) {
         background: oklch(0.98 0.01 50 / 0.5);
       }
       @media (prefers-color-scheme: dark) {
         .bazi-report tr:nth-child(even) {
           background: oklch(0.25 0.01 50 / 0.5);
         }
       }
       .bazi-report ul, .bazi-report ol {
         margin: 0.75rem 0;
         padding-left: 1.5rem;
       }
       .bazi-report li {
         margin: 0.25rem 0;
       }
     </style>
   </head>
   <body>
     <div class="bazi-report" style="max-width: 800px; margin: 2rem auto; padding: 2rem; background: var(--card); border-radius: 0.5rem;">
       ${content}
     </div>
     <footer style="text-align: center; margin-top: 2rem; color: var(--foreground); opacity: 0.7; font-size: 0.875rem;">
       —— 喵十七 · 八字命理
     </footer>
   </body>
   </html>
   ```
   - Note: The content passed in is already sanitized by BaziReportTab's DOMPurify, so no additional sanitization needed here
  </action>
  <verify>
    <automated>grep -c "export.*downloadReportAsHtml\|export.*buildDownloadTemplate" src/lib/bazi/download.ts && grep -c "cdn.jsdelivr.net/npm/lxgw-wenkai-webfont" src/lib/bazi/download.ts</automated>
  </verify>
  <done>BaziReport can import downloadReportAsHtml and call it to trigger download</done>
</task>

<task type="auto">
  <name>Task 2: Add download button to BaziReport tab bar</name>
  <files>src/components/bazi/BaziReport.tsx</files>
  <read_first>
    - src/components/bazi/BaziReport.tsx
    - src/lib/bazi/download.ts (the file created in Task 1)
  </read_first>
  <action>
Modify src/components/bazi/BaziReport.tsx:

1. Import the download function at the top:
   ```typescript
   import { downloadReportAsHtml } from "@/lib/bazi/download";
   ```

2. Add a download button to the tab bar section (after the two tab buttons, before the content area):
   - The button should be placed to the right of the tabs
   - Use a flex layout on the tab bar: tabs on left, download button on right
   - Button text: "下载报告"
   - Button style: matching the secondary button style, small size
   - Icon: a download arrow SVG or simple text "↓"

   Modify the tab bar div from:
   ```jsx
   <div className="flex border-b border-[var(--border)]">
   ```
   To:
   ```jsx
   <div className="flex items-center justify-between border-b border-[var(--border)]">
     <div className="flex">
       {/* existing tab buttons */}
     </div>
     <button
       onClick={() => downloadReportAsHtml(
         activeTab === 'professional' ? professional : detailed,
         activeTab,
         inputName
       )}
       className="mr-3 px-3 py-1.5 text-xs text-[var(--muted-foreground)] hover:text-[var(--foreground)] hover:bg-[var(--secondary)] rounded transition-colors flex items-center gap-1"
     >
       <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
         <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
       </svg>
       下载报告
     </button>
   </div>
   ```

   The button is placed per D-01: "下载报告 button in BaziReport's tab bar, right side of 专业版/详解版 tabs"
  </action>
  <verify>
    <automated>grep -c "downloadReportAsHtml" src/components/bazi/BaziReport.tsx && grep -c "下载报告" src/components/bazi/BaziReport.tsx</automated>
  </verify>
  <done>Download button visible on report, clicking it triggers browser download dialog</done>
</task>

<task type="auto">
  <name>Task 3: Add scoped CSS styles to BaziReportTab for in-page rendering</name>
  <files>src/components/bazi/BaziReportTab.tsx</files>
  <read_first>
    - src/components/bazi/BaziReportTab.tsx
    - src/app/globals.css (for design token values)
  </read_first>
  <action>
Modify src/components/bazi/BaziReportTab.tsx to inject scoped CSS styles before the content:

1. Add a `<style>` tag at the start of the component's return that mirrors the download template styles but scoped to `.bazi-report` class:
   ```tsx
   return (
     <>
       <style>{`
         .bazi-report h2 {
           font-family: var(--font-brand);
           font-size: 1.5rem;
           font-weight: 700;
           color: var(--primary);
           margin: 1.5rem 0 0.75rem;
         }
         .bazi-report h3 {
           font-size: 1.125rem;
           font-weight: 600;
           margin: 1.25rem 0 0.5rem;
         }
         .bazi-report p {
           margin: 0.75rem 0;
           line-height: 1.7;
         }
         .bazi-report table {
           width: 100%;
           border-collapse: collapse;
           margin: 1rem 0;
         }
         .bazi-report th, .bazi-report td {
           border: 1px solid var(--border);
           padding: 0.5rem 0.75rem;
           text-align: left;
         }
         .bazi-report th {
           background: var(--primary);
           color: white;
           font-weight: 600;
         }
         .bazi-report tr:nth-child(even) {
           background: oklch(0.98 0.01 50 / 0.5);
         }
         @media (prefers-color-scheme: dark) {
           .bazi-report tr:nth-child(even) {
             background: oklch(0.25 0.01 50 / 0.5);
           }
         }
         .bazi-report ul, .bazi-report ol {
           margin: 0.75rem 0;
           padding-left: 1.5rem;
         }
         .bazi-report li {
           margin: 0.25rem 0;
         }
       `}</style>
       <div
         className="bazi-report text-[var(--foreground)] leading-relaxed space-y-4"
         dangerouslySetInnerHTML={{ __html: cleanContent }}
       />
     </>
   );
   ```

   This implements D-04: "BaziReportTab injects a <style> tag before the content, scoped to .bazi-report class"
   And D-05: "Style rules use project CSS variables"
   And D-06: "Target selectors: h2 (font-brand, text-2xl, primary color), h3 (font-semibold), table (borders, header bg), p (line-height), ul/ol/li"
  </action>
  <verify>
    <automated>grep -c "\.bazi-report h2\|\.bazi-report h3\|\.bazi-report table" src/components/bazi/BaziReportTab.tsx</automated>
  </verify>
  <done>Report headings have primary color, tables have borders, dark mode works correctly</done>
</task>

</tasks>

<verification>
## Phase 10 Verification

### Functional Verification
1. **Download button exists**: Button with "下载报告" text visible in BaziReport tab bar
2. **Download triggers**: Clicking download creates a .html file download
3. **Filename format**: File named `八字报告-{name}-{version}.html`
4. **CDN fonts load**: Downloaded HTML references LXGW WenKai from jsDelivr CDN

### Visual Verification (Human Checkpoint)
5. **Report headings**: h2 elements use primary color and larger font
6. **Table styling**: Tables have borders, header row has primary background
7. **Dark mode**: Report renders correctly in dark mode with proper colors

### Build Verification
8. `npm run build` passes with zero TypeScript errors
</verification>

<success_criteria>
1. User can download report as HTML file via button click
2. Downloaded HTML uses CDN fonts and requires no external dependencies (except CDN)
3. Downloaded HTML follows project design language (warm minimalist aesthetic)
4. Report headings have clear visual hierarchy with primary color
5. Report tables have borders and alternating row styles using design tokens
6. `npm run build` passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-download-polish/10-01-SUMMARY.md`
</output>
