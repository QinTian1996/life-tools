---
phase: "06"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/MessageBubble.tsx
  - src/components/chat/ChatInput.tsx
  - src/components/chat/MessageList.tsx
autonomous: true
requirements:
  - MIGR-02
must_haves:
  truths:
    - "User message bubbles use primary tokens (bg + foreground)"
    - "AI message bubbles use secondary tokens (bg + foreground)"
    - "ChatInput send button uses Button component"
    - "ChatInput textarea uses design tokens for border, ring, text"
    - "ChatInput error display uses destructive tokens"
    - "MessageList loading dots use primary token"
  artifacts:
    - path: src/components/chat/MessageBubble.tsx
      provides: Semantic token styling for user (primary) and AI (secondary) messages
    - path: src/components/chat/ChatInput.tsx
      provides: Button send, token textarea, destructive error
    - path: src/components/chat/MessageList.tsx
      provides: Loading dots with primary token
  key_links:
    - from: MessageBubble
      to: src/app/globals.css
      via: CSS variables bg-[var(--primary)], bg-[var(--secondary)]
      pattern: var\(--primary\)|var\(--secondary\)
    - from: ChatInput
      to: src/components/ui/Button
      via: import
      pattern: import.*Button
---

<objective>
Migrate all three chat components (MessageBubble, ChatInput, MessageList) to use design system tokens. This completes MIGR-02.
</objective>

<context>
@src/components/chat/MessageBubble.tsx
@src/components/chat/ChatInput.tsx
@src/components/chat/MessageList.tsx
@src/components/ui/Button.tsx
@.planning/phases/06-page-migration-verification/06-CONTEXT.md

**D-02:** ChatInput → send button `<Button>`, textarea uses `var(--input)`, `var(--ring)`, `var(--foreground)`
**D-03:** MessageBubble → user: `var(--primary)`/`var(--primary-foreground)`, AI: `var(--secondary)`/`var(--secondary-foreground)`
**D-04:** ChatInput error → `var(--destructive)`/`var(--destructive-foreground)`
**D-05:** MessageList loading dots → `var(--primary)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MessageBubble to semantic tokens</name>
  <files>src/components/chat/MessageBubble.tsx</files>
  <action>
    Edit src/components/chat/MessageBubble.tsx:

    Replace the className in the inner div (lines 22-27):

    OLD (lines 22-27):
    ```
    className={`max-w-[80%] px-4 py-3 rounded-lg ${
      message.role === 'assistant'
        ? 'bg-orange-50 text-gray-800 rounded-br-none'
        : 'bg-amber-500 text-white rounded-bl-none'
    }`}
    ```

    NEW:
    ```
    className={`max-w-[80%] px-4 py-3 rounded-lg ${
      message.role === 'assistant'
        ? 'bg-[var(--secondary)] text-[var(--secondary-foreground)] rounded-br-none'
        : 'bg-[var(--primary)] text-[var(--primary-foreground)] rounded-bl-none'
    }`}
    ```

    User messages (role === 'user'): `bg-[var(--primary)] text-[var(--primary-foreground)]`
    AI messages (role === 'assistant'): `bg-[var(--secondary)] text-[var(--secondary-foreground)]`

    The `rounded-bl-none` and `rounded-br-none` are tailwind border-radius suppression classes - keep them.
  </action>
  <verify>
    <automated>grep -c "bg-\[var\(--primary\)\]" src/components/chat/MessageBubble.tsx && grep -c "bg-\[var\(--secondary\)\]" src/components/chat/MessageBubble.tsx</automated>
  </verify>
  <done>
    MessageBubble renders user messages with primary tokens and AI messages with secondary tokens. Light/dark switching automatic via CSS variable system.
  </done>
  <read_first>src/components/chat/MessageBubble.tsx</read_first>
  <acceptance_criteria>
    - grep returns 1 for `bg-\[var\(--primary\)\]` in MessageBubble.tsx
    - grep returns 1 for `bg-\[var\(--secondary\)\]` in MessageBubble.tsx
    - grep returns 1 for `text-\[var\(--primary-foreground\)\]` in MessageBubble.tsx
    - grep returns 1 for `text-\[var\(--secondary-foreground\)\]` in MessageBubble.tsx
    - grep returns 0 for `bg-orange-50` in MessageBubble.tsx
    - grep returns 0 for `bg-amber-500` in MessageBubble.tsx
    - grep returns 0 for `text-gray-800` in MessageBubble.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate ChatInput - Button send + textarea tokens + error tokens</name>
  <files>src/components/chat/ChatInput.tsx</files>
  <action>
    Edit src/components/chat/ChatInput.tsx:

    1. Add import for Button at top (after 'use client'):

    OLD (line 1-2):
    ```
    'use client';

    interface ChatInputProps {
    ```

    NEW:
    ```
    'use client';

    import { Button } from "@/components/ui/Button";

    interface ChatInputProps {
    ```

    2. Replace error div (lines 35-39) with destructive tokens:

    OLD:
    ```
    <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-2 rounded-lg mb-2 text-sm">
      {error.message || '发生错误，请重试'}
    </div>
    ```

    NEW:
    ```
    <div className="bg-[var(--destructive)] text-[var(--destructive-foreground)] border border-[var(--destructive)] px-4 py-2 rounded-lg mb-2 text-sm">
      {error.message || '发送失败，请重试'}
    </div>
    ```

    3. Replace textarea className (line 47):

    OLD:
    ```
    className="flex-1 border border-gray-200 rounded-lg px-4 py-3 resize-none focus:outline-none focus:ring-2 focus:ring-amber-500 text-gray-800"
    ```

    NEW:
    ```
    className="flex-1 border border-[var(--input)] rounded-[var(--radius-md)] px-4 py-3 resize-none focus:outline-none focus:border-[var(--ring)] focus:ring-2 focus:ring-[var(--ring)] text-[var(--foreground)] bg-transparent"
    ```

    Note: Use `rounded-[var(--radius-md)]` for consistency with Input component. The `bg-transparent` ensures no hardcoded white bg.

    4. Replace send button (lines 51-57) with Button component:

    OLD:
    ```
    <button
      onClick={handleSend}
      disabled={isLoading || !input.trim()}
      className="px-4 py-2 bg-amber-600 hover:bg-amber-700 disabled:bg-amber-300 text-white rounded-lg font-medium transition-colors"
    >
      发送
    </button>
    ```

    NEW:
    ```
    <Button
      onClick={handleSend}
      disabled={isLoading || !input.trim()}
      variant="primary"
      size="default"
    >
      发送
    </Button>
    ```

    Note: The Button component handles hover/active states internally. No need for explicit transition-colors on the Button element.
  </action>
  <verify>
    <automated>grep -c "import.*Button" src/components/chat/ChatInput.tsx && grep -c "bg-\[var\(--destructive\)\]" src/components/chat/ChatInput.tsx</automated>
  </verify>
  <done>
    ChatInput uses Button component for send, textarea uses design tokens, error uses destructive tokens.
  </done>
  <read_first>src/components/chat/ChatInput.tsx, src/components/ui/Button.tsx</read_first>
  <acceptance_criteria>
    - grep returns 1 for `import.*Button` in ChatInput.tsx
    - grep returns 1 for `Button` (opening tag) in ChatInput.tsx
    - grep returns 1 for `bg-\[var\(--destructive\)\]` in ChatInput.tsx
    - grep returns 1 for `text-\[var\(--destructive-foreground\)\]` in ChatInput.tsx
    - grep returns 1 for `border-\[var\(--input\)\]` in ChatInput.tsx
    - grep returns 1 for `focus:ring-\[var\(--ring\)\]` in ChatInput.tsx
    - grep returns 1 for `text-\[var\(--foreground\)\]` in ChatInput.tsx
    - grep returns 0 for `bg-amber-600` in ChatInput.tsx
    - grep returns 0 for `bg-red-50` in ChatInput.tsx
    - grep returns 0 for `border-gray-200` in ChatInput.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Migrate MessageList loading dots to primary token</name>
  <files>src/components/chat/MessageList.tsx</files>
  <action>
    Edit src/components/chat/MessageList.tsx:

    Replace the three loading dot `bg-amber-600` classes (lines 45, 52, 59):

    OLD (line 45):
    ```
    className="inline-block w-2 h-2 bg-amber-600 rounded-full"
    ```
    OLD (line 52):
    ```
    className="inline-block w-2 h-2 bg-amber-600 rounded-full"
    ```
    OLD (line 59):
    ```
    className="inline-block w-2 h-2 bg-amber-600 rounded-full"
    ```

    NEW (all three):
    ```
    className="inline-block w-2 h-2 bg-[var(--primary)] rounded-full"
    ```

    The three dots have different animationDelay values (0s, 0.16s, 0.32s) - preserve those inline styles.
    The bouncing animation itself uses transform so we only need to change the bg color.
  </action>
  <verify>
    <automated>grep -c "bg-\[var\(--primary\)\]" src/components/chat/MessageList.tsx && grep -c "bg-amber-600" src/components/chat/MessageList.tsx</automated>
  </verify>
  <done>
    MessageList loading dots use primary token instead of hardcoded amber-600. Dots switch to amber-400 in dark mode automatically.
  </done>
  <read_first>src/components/chat/MessageList.tsx</read_first>
  <acceptance_criteria>
    - grep returns 3 for `bg-\[var\(--primary\)\]` in MessageList.tsx
    - grep returns 0 for `bg-amber-600` in MessageList.tsx
  </acceptance_criteria>
</task>

</tasks>

<verification>
npm run build must pass. No TypeScript errors.
</verification>

<success_criteria>
- MessageBubble: user uses primary tokens, AI uses secondary tokens
- ChatInput: Button for send, textarea uses input/ring/foreground tokens
- ChatInput error: destructive tokens
- MessageList loading dots: primary token
- No hardcoded colors (bg-amber-600, bg-orange-50, bg-red-50, text-gray-800, border-gray-200) remain in chat components
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-migration-verification/06-02-SUMMARY.md`
</output>
