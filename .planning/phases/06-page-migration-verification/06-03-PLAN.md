---
phase: "06"
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified: []
autonomous: true
requirements:
  - MIGR-03
  - MIGR-04
must_haves:
  truths:
    - "npm run build passes without errors"
    - "Brand bar is semi-transparent warm (not pure black) in dark mode"
    - "Text is readable in both light and dark modes"
    - "No hardcoded color values remain on migrated pages"
    - "Chat functionality is unaffected"
  artifacts:
    - path: .planning/phases/06-page-migration-verification/VERIFICATION.md
      provides: Post-migration verification checklist with build + visual checks
  key_links:
    - from: src/components/layout/SiteHeader.tsx
      to: src/app/globals.css
      via: --header-bg variable
      pattern: var\(--header-bg\)
---

<objective>
Verify the migrated pages pass build and visual checks. This completes MIGR-03 (light mode) and MIGR-04 (dark mode).
</objective>

<context>
@src/components/layout/SiteHeader.tsx
@src/app/globals.css
@.planning/phases/06-page-migration-verification/06-CONTEXT.md (D-07: verification checklist)
@.planning/phases/06-page-migration-verification/06-UI-SPEC.md (verification checklist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm run build to verify no errors</name>
  <files>[]</files>
  <action>
    Run the build command in the project directory:

    ```
    npm run build
    ```

    This verifies:
    - All TypeScript types are correct
    - All imports resolve correctly
    - No build-time errors in the migrated pages
    - Chat functionality (API routes) still works

    If build fails, fix the errors before proceeding to Task 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build completes successfully with no errors.
  </done>
  <read_first>src/app/page.tsx, src/components/chat/ChatInput.tsx, src/components/chat/MessageBubble.tsx, src/components/chat/MessageList.tsx</read_first>
  <acceptance_criteria>
    - npm run build exits with code 0
    - Output contains "Compiled successfully" or similar success indicator
    - No TypeScript errors in output
    - No import errors in output
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create verification checklist document</name>
  <files>[.planning/phases/06-page-migration-verification/VERIFICATION.md]</files>
  <action>
    Create a verification checklist document at:

    `.planning/phases/06-page-migration-verification/VERIFICATION.md`

    With the following content:

    ```markdown
    # Phase 6: Migration Verification Checklist

    **Date:** 2026-05-10
    **Status:** POST-MIGRATION

    ## Build Verification

    - [ ] `npm run build` passes with no errors
    - [ ] No TypeScript errors
    - [ ] No import resolution errors

    ## Visual Verification (Light Mode)

    - [ ] PageLayout background is warm-stone-50 (light warm background)
    - [ ] Home page title "喵十七的工具箱" uses font-brand, correct size (text-3xl)
    - [ ] Home page subtitle uses muted-foreground color
    - [ ] Three link-buttons render with correct variants:
      - 算八字: primary (amber) background
      - 今天吃什么: secondary (warm-stone) background
      - 聊天: primary (amber) background
    - [ ] Chat page MessageBubble: user = primary (amber), AI = secondary (warm-stone)
    - [ ] ChatInput textarea has correct border color
    - [ ] ChatInput send button renders with Button component

    ## Visual Verification (Dark Mode)

    - [ ] Add `?dark` query param or toggle dark mode
    - [ ] Brand bar is `rgba(28, 25, 23, 0.85)` with blur - NOT pure black
    - [ ] PageLayout background is warm-stone-900 (dark warm background, not pure black)
    - [ ] Home page title is readable (warm-stone-100 text)
    - [ ] Home page subtitle is muted-foreground (warm-stone-400)
    - [ ] Three link-buttons switch to dark variants:
      - 算八字: primary (amber-400) background
      - 今天吃什么: secondary (warm-stone-800) background
      - 聊天: primary (amber-400) background
    - [ ] Chat page MessageBubble: user = primary (amber-400), AI = secondary (warm-stone-800)
    - [ ] Loading dots use primary token (amber)
    - [ ] All text is readable - no contrast issues

    ## Token Compliance Check

    Run these greps to verify no hardcoded colors remain:

    ```bash
    # Should return 0 for migrated files
    grep -c "bg-amber-600\|bg-orange-50\|bg-red-50\|text-gray-800\|border-gray-200" src/app/page.tsx
    grep -c "bg-amber-600\|bg-orange-50\|bg-red-50" src/components/chat/MessageBubble.tsx
    grep -c "bg-amber-600\|bg-red-50\|border-gray-200" src/components/chat/ChatInput.tsx
    grep -c "bg-amber-600" src/components/chat/MessageList.tsx
    grep -c "bg-gradient-to-b\|from-amber-50\|to-orange-50" src/components/layout/PageLayout.tsx
    ```

    All should return 0.

    ## Chat Functionality Check

    - [ ] Can send a message in chat
    - [ ] AI response appears
    - [ ] Streaming works (messages appear progressively)
    - [ ] Error state displays correctly (destructive tokens)

    ## Sign-Off

    - [ ] All checks passed
    - [ ] MIGR-01: Home page migrated
    - [ ] MIGR-02: Chat page migrated
    - [ ] MIGR-03: Light mode verified
    - [ ] MIGR-04: Dark mode verified
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/06-page-migration-verification/VERIFICATION.md && echo "exists"</automated>
  </verify>
  <done>
    Verification checklist created and completed (user must manually verify visual aspects).
  </done>
  <read_first>src/components/layout/SiteHeader.tsx, src/app/globals.css</read_first>
  <acceptance_criteria>
    - VERIFICATION.md file exists
    - Contains build verification section
    - Contains light mode visual checklist
    - Contains dark mode visual checklist
    - Contains token compliance grep commands
    - Contains chat functionality checklist
  </acceptance_criteria>
</task>

</tasks>

<verification>
npm run build passes. Visual checklist completed manually.
</verification>

<success_criteria>
- npm run build passes with exit code 0
- VERIFICATION.md exists with complete checklist
- All hardcoded colors removed from migrated pages (verified via grep)
- Chat functionality unchanged (build confirms no API breakage)
- Visual verification completed by user (brand bar warm in dark, text readable both modes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-migration-verification/06-03-SUMMARY.md`
</output>
