---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/PageLayout.tsx
autonomous: true
requirements:
  - MIGR-01
must_haves:
  truths:
    - "PageLayout uses solid warm background instead of gradient"
    - "Home page displays three link-buttons using Button component with asChild"
    - "Home page title and subtitle use font-brand with correct sizes/weights"
  artifacts:
    - path: src/components/layout/PageLayout.tsx
      provides: Solid warm-stone background (light: warm-stone-50, dark: warm-stone-900)
      contains: bg-[var(--background)]
    - path: src/app/page.tsx
      provides: Three link-buttons with Button asChild + Link
      contains: Button asChild Link
  key_links:
    - from: src/app/page.tsx
      to: src/components/ui/Button
      via: import
      pattern: from "@/components/ui/Button"
    - from: PageLayout
      to: SiteHeader
      via: import
      pattern: import.*SiteHeader
---

<objective>
Migrate PageLayout background from gradient to solid, and home page link-buttons to Button component with asChild pattern. This completes MIGR-01.
</objective>

<context>
@src/components/layout/PageLayout.tsx
@src/app/page.tsx
@src/components/ui/Button.tsx
@.planning/phases/06-page-migration-verification/06-CONTEXT.md

**D-01:** Home link-buttons → `<Button asChild><Link>` (variant: primary)
**D-06:** PageLayout background → `bg-[var(--background)]` (solid, not gradient)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PageLayout background from gradient to solid</name>
  <files>src/components/layout/PageLayout.tsx</files>
  <action>
    Edit src/components/layout/PageLayout.tsx:

    Replace line 10:
    OLD: `className="min-h-screen flex flex-col bg-gradient-to-b from-amber-50 to-orange-50"`
    NEW: `className="min-h-screen flex flex-col bg-[var(--background)]"`

    The `bg-gradient-to-b from-amber-50 to-orange-50` is hardcoded warm gradient.
    The `bg-[var(--background)]` uses the design token which is warm-stone-50 in light mode and warm-stone-900 in dark mode.
  </action>
  <verify>
    <automated>grep -c "bg-\[var(--background)\]" src/components/layout/PageLayout.tsx</automated>
  </verify>
  <done>
    PageLayout renders with solid warm background. Token switches automatically between light (warm-stone-50) and dark (warm-stone-900) mode.
  </done>
  <read_first>src/components/layout/PageLayout.tsx</read_first>
  <acceptance_criteria>
    - grep returns 1 for `bg-\[var(--background)\]` in PageLayout.tsx
    - No `bg-gradient-to-b` or `from-amber-50` or `to-orange-50` remains in PageLayout.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate home page link-buttons to Button asChild pattern</name>
  <files>src/app/page.tsx</files>
  <action>
    Edit src/app/page.tsx:

    1. Add import for Button: `import { Button } from "@/components/ui/Button";` after the Link import

    2. Replace the three `<Link>` elements (lines 11-28) with `<Button asChild><Link>` pattern:

    OLD (lines 11-28):
    ```
    <Link
      href="/bazi"
      className="px-6 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition-colors"
    >
      算八字
    </Link>
    <Link
      href="/eat"
      className="px-6 py-3 bg-orange-500 text-white rounded-lg hover:bg-orange-600 transition-colors"
    >
      今天吃什么
    </Link>
    <Link
      href="/chat"
      className="px-6 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition-colors"
    >
      聊天
    </Link>
    ```

    NEW:
    ```
    <Button asChild variant="primary" size="default">
      <Link href="/bazi">算八字</Link>
    </Button>
    <Button asChild variant="secondary" size="default">
      <Link href="/eat">今天吃什么</Link>
    </Button>
    <Button asChild variant="primary" size="default">
      <Link href="/chat">聊天</Link>
    </Button>
    ```

    Note: "今天吃什么" uses secondary variant (warm-stone-100 bg in light, warm-stone-800 in dark).
    The other two use primary variant (amber bg).

    3. Update title to use font-brand token: Change `text-4xl font-bold text-amber-900` to `text-3xl font-[family-name:var(--font-brand)] font-semibold text-[var(--foreground)]`

    4. Update subtitle to use token: Change `text-amber-700 text-lg` to `text-[var(--muted-foreground)] text-lg`

    5. Update footer text: Change `text-amber-400 text-sm` to `text-[var(--muted-foreground)] text-sm`
  </action>
  <verify>
    <automated>grep -c "asChild" src/app/page.tsx && grep -c "Button" src/app/page.tsx | head -1</automated>
  </verify>
  <done>
    Home page renders three link-buttons using Button component with asChild. Title/subtitle use font-brand and tokens.
  </done>
  <read_first>src/app/page.tsx, src/components/ui/Button.tsx</read_first>
  <acceptance_criteria>
    - grep returns 3 for `asChild` in src/app/page.tsx
    - grep returns 1 for `import { Button }` in src/app/page.tsx
    - grep returns 0 for `bg-amber-600` in src/app/page.tsx
    - grep returns 0 for `bg-orange-500` in src/app/page.tsx
    - grep returns 1 for `font-\[family-name:var\(--font-brand\)\]` in src/app/page.tsx
    - grep returns 1 for `text-\[var\(--foreground\)\]` in src/app/page.tsx
  </acceptance_criteria>
</task>

</tasks>

<verification>
npm run build must pass. No TypeScript errors.
</verification>

<success_criteria>
- PageLayout uses solid `bg-[var(--background)]` instead of gradient
- Home page link-buttons use `<Button asChild><Link>` pattern
- Home page title uses `font-[family-name:var(--font-brand)]` and `text-[var(--foreground)]`
- Home page subtitle uses `text-[var(--muted-foreground)]`
- No hardcoded amber-600, orange-500 colors remain in page.tsx
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-migration-verification/06-01-SUMMARY.md`
</output>
